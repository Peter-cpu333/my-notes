import React from 'react';
import CustomColorModeToggle from '@site/src/components/theme/CustomColorModeToggle';

export default function ColorModeToggleWrapper(props) {
  return <CustomColorModeToggle {...props} />;
}
